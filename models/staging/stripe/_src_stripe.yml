sources:
  - name: stripe
    database: orclpdb
    schema: dbt_user
    tables:
      - name: raw_payments
        config:
          loaded_at_field: created
          freshness:
            warn_after: {count: 12, period: hour}
            error_after: {count: 24, period: hour}
